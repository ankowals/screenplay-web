<!DOCTYPE aspectj PUBLIC "-//AspectJ//DTD//EN" "http://www.eclipse.org/aspectj/dtd/aspectj.dtd">
<aspectj>
    <weaver>
        <!-- only weave classes in our application-specific packages -->
        <include within="pom..*" />
        <include within="screenplay..*" />
        <include within="tests..*" />
        <include within="reporting..*" />
    </weaver>
    <aspects>
        <!-- weave in just this aspects -->
        <concrete-aspect name="framework.reporting.aspects.ConcreteLogAspect" extends="framework.reporting.aspects.AbstractLogAspect">
            <pointcut name="scope" expression="(execution(* pom..*(..)) AND !execution(* pom.framework..*(..)))
            || (execution(* screenplay..*(..)) AND !execution(* screenplay.framework..*(..)))"/>
        </concrete-aspect>
        <aspect name="framework.reporting.aspects.TestExceptionLoggerAspect"/>
    </aspects>
</aspectj>